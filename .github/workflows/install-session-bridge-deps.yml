name: Install Session Bridge Dependency

on:
  push:
    branches:
      - feat/modo-sessao-temporaria-2026
    paths:
      - package.json

permissions:
  contents: write

jobs:
  install:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/modo-sessao-temporaria-2026
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm install --package-lock-only --ignore-scripts
      - run: npm ci
      - run: npm run check
      - name: Publicar lockfile e remover workflow temporario
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/install-session-bridge-deps.yml
          git add package-lock.json
          git commit -m "build: integra dependencia da ponte de sessao"
          git push origin HEAD:feat/modo-sessao-temporaria-2026
