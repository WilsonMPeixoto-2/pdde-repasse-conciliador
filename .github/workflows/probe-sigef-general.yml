name: Probe SIGEF General
on:
  push:
    branches:
      - test/sigef-month-window-2344
    paths:
      - 'scripts/probe-sigef-general.ts'
      - '.github/workflows/probe-sigef-general.yml'
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Inspecionar consulta geral de extratos
        run: node --import tsx scripts/probe-sigef-general.ts
